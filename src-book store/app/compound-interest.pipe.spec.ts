import { CompoundInterestPipe } from './compound-interest.pipe';

describe('CompoundInterestPipe', () => {
  it('create an instance', () => {
    const pipe = new CompoundInterestPipe();
    expect(pipe).toBeTruthy();
  });
});
